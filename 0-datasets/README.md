# Datasets

This course uses datasets loaded at runtime from `sklearn.datasets` and `seaborn`.
No local dataset files are needed — all data is fetched programmatically in the notebooks.

See `DIDAKTIK.md` for the full dataset inventory.
