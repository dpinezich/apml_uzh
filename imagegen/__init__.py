"""APML UZH — per-chapter image & GIF generators.

Each ``imagegen/chNN.py`` exposes ``generate()`` and is called from
``generate_images.py``.  Shared style, colours and save helpers live in
``imagegen.common``.
"""
